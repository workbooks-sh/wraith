---
paths:
  - "crates/core/src/analyze/**"
  - "crates/extract/src/visitor/**"
  - "crates/graph/src/graph/**"
  - "crates/graph/src/resolve/**"
---

# Detection capabilities

Non-obvious implementation details for each detection feature. These are NOT discoverable from reading the code alone.

## Parser-level
- **HTML entry files**: `<script src="...">` (module and classic) and `<link rel="stylesheet" href="...">` / `<link rel="modulepreload" href="...">` create graph edges from HTML to referenced assets. Remote URLs skipped. HTML comments stripped before matching. HTML files exempt from unused-file detection.
- **JSX asset references**: lowercase intrinsic `<script src="...">` and `<link rel="stylesheet|modulepreload" href="...">` inside JSX/TSX files create `SideEffect` imports, mirroring the HTML parser. Only `JSXAttributeValue::StringLiteral` values are captured; expression containers (`href={expr}`) and capitalized React-style components (`<Script>`, `<Link>`) are ignored. Needed for SSR frameworks like Hono where layout components emit HTML via JSX. Combined with the web-root-relative resolver branch, `<link href="/static/style.css" />` correctly reaches sibling `static/style.css` files. See issue #105 (till's comment).
- **`html` tagged template asset references**: `` html`<script src="..."></script>` `` and `` html`<link rel="stylesheet|modulepreload" href="..."/>` `` inside any JS/TS file create `SideEffect` imports via the regex-based HTML asset scanner (`crate::html::collect_asset_refs`). Only the bare identifier tag `html` is matched — `css`, `sql`, `gql`, `styled.div`, and member/call expressions are deliberately ignored to avoid misreading unrelated tagged templates as HTML. Each template quasi is scanned independently, so an asset reference split across an interpolation boundary (`` html`<script src="${base}/app.js">` ``) is skipped rather than producing a garbled unresolvable specifier. Needed for SSR helpers like `hono/html`, `lit-html`, and `htm` where layout components emit HTML via tagged templates instead of JSX. See issue #105 (till's follow-up comment).
- **GraphQL document imports**: `.graphql` and `.gql` files are discovered as source files. Lines matching `#import "./fragment.graphql"` or `# import '../shared/fragment.gql'` create `SideEffect` imports so fragment/schema documents reachable only through GraphQL import comments stay connected in the module graph. Only relative string specifiers (`./`, `../`) are extracted; package-style imports are ignored. Extensionless relative imports probe `.graphql` and `.gql` through the resolver extension list. See issue #250.
- **JSDoc `import()` type references**: JSDoc comments matching `import('./path').Member` inside `@param`, `@returns`, `@type`, `@typedef`, `@callback`, or other tag contexts are extracted as type-only imports. Supports single/double quotes, bare specifiers, parent-relative paths, union annotations with multiple `import()` expressions per comment, and nested member access (first segment wins). Side-effect-only `import('./types')` without `.Member` is recorded as `SideEffect` to keep the file reachable. Prevents false `unused-export` reports for types referenced only from JSDoc annotations in untyped JS files. Only `/** */` block comments are scanned (single-star `/* */` is not JSDoc). See issue #105 (till's comment).
- **`typeof import('./path').X` in TypeScript type positions**: `TSImportType` AST nodes are extracted as type-only `ImportInfo` entries. A flat qualifier (`typeof import('./x').foo`) credits a `Named("foo")` import; a nested qualifier (`typeof import('./x').A.B`) walks to the leftmost identifier and credits `Named("A")`; no qualifier and the indexed-access form (`typeof import('./x')['default']`) record a `SideEffect` import so the target file stays reachable. Fires inside any type position, including the bodies of `declare global { ... }` and `declare module 'pkg' { ... }` ambient declarations produced by unplugin-auto-import (`auto-imports.d.ts`) and unplugin-vue-components (`components.d.ts`). Pairs with the graph-level promotion of every `.d.ts` / `.d.mts` / `.d.cts` file to an overall reachability root (`ModuleGraph::build_with_reachability_roots` augments `entry_point_ids` with declaration files), so the new edges propagate through BFS even when the d.ts file is not in the user's `entry` config. Side effect: `is_entry_point()` returns `true` for declaration files, which auto-skips them in `unused-exports` / `unused-types` detection (preventing false positives on module-augmentation interfaces TypeScript consumes via global type resolution); pass `--include-entry-exports` to re-enable. Runtime / test reachability is unchanged because declaration files have no runtime side effects. See issues #396 and #397.
- **`new URL('./', import.meta.url)` directory specifiers skipped**: the worker / asset detector for `new URL(literal, import.meta.url)` admits relative specifiers (`./`, `../` prefixes) but rejects directory-only forms (`./`, `../`, `./foo/`). Directory URLs do not resolve to a module, so reporting them as `unresolved-imports` was a false positive on the canonical ESM `__dirname` idiom (`fileURLToPath(new URL('./', import.meta.url))`). File-pointing specifiers (`./worker.js`, `./assets/foo.svg`) are unaffected. See issue #399.
- **Vue/Svelte SFC**: handles `>` in quoted attributes like `generic="T extends Foo<Bar>"`, `<script src="...">` external script support, HTML comment filtering. Template-visible import tracking: imports used only in markup (`{formatDate(x)}`, `utils.formatDate()`) are credited as used, preventing false unused-import/export reports. Vue credits only `<script setup>` bindings; Svelte excludes `context="module"` scripts from template visibility. Namespace member access in templates (`utils.formatDate`) tracked as member usage.
- **Vue/Svelte SFC `<style>` blocks**: `<style lang="scss">` and `<style lang="sass">` block bodies are scanned for `@import` / `@use` / `@forward` / Tailwind CSS `@plugin` directives via the same regexes used for plain `.scss` files (issue #195 Case B). Plain `<style>` (no `lang`) and `<style lang="css">` are scanned for `@import` and `@plugin`. `<style src="./theme.scss">` external style references are extracted symmetrically with `<script src>` and emit a `SideEffect` import on the SFC. Each style-context import is tagged with `from_style: true` on `ImportInfo` so the resolver routes through the SCSS fallback chain (CSS-extension probe, partial convention `_filename`, include paths, node_modules) BEFORE the standard JS resolver. Without that routing, `./Foo` from a `Foo.vue` style block would resolve to the SFC itself (`.vue` precedes `.scss` in the resolver's extension list); the CSS-restricted probe selects the sibling `Foo.scss` instead. Languages other than CSS / SCSS / Sass (`postcss`, `less`, `stylus`) only have their `<style src>` reference seeded; their body syntax differs (e.g., Less `@import (reference)` modifier) and is out of scope.
- **Glimmer component files**: `.gts` and `.gjs` files are discovered as source files. `.gts` parses with TypeScript source semantics and `.gjs` parses with JavaScript module semantics. `<template>...</template>` blocks are stripped before Oxc parsing while preserving byte offsets and line numbers, so imports before/around templates are still extracted and resolved through the same tsconfig path-alias path as `.ts` importers. Two replacement strategies are used so the post-strip source stays syntactically valid in both canonical GTS contexts: class-body templates (`class X { <template>...</template> }`) blank to spaces, leaving an empty class body; module-level template expressions (`const X = <template>...</template>`) and other expression-position templates (function argument, ternary branch, decorator-call argument) are replaced with a byte-length-preserving parenthesized template literal `` (`...`) ``. Without the expression-context branch, a file mixing both shapes parses to `const x = ;`, oxc bails, and every import in the file is dropped. Expression position is detected by the previous non-whitespace byte being one of `=`, `,`, `(`, `?`, `:`, plus an identifier-byte lookback that matches the expression-prefix keyword set `{default, return, throw, yield, await, new}` so the canonical template-only-component shape `export default <template>...</template>` (no `const` wrapper) does not blank to `export default ;`. User identifiers ending in a keyword suffix (`mydefault`, `$return`) walk back the FULL identifier and do not match. See issues #270, #375, #379.
- **Namespace destructuring**: `const { a, b } = ns` → member accesses. Rest patterns (`const { foo, ...rest } = ns`) → conservative whole-object use. Works with static/dynamic imports and require.
- **Node Module Customization Hooks (`register()`)**: `register('@swc-node/register/esm', pathToFileURL('./'))` calls (Node.js `module.register()`) credit the first-argument specifier as a `DynamicImportInfo`. The package referenced by the specifier is no longer reported as unused. Gated by an imports-table lookup (`is_node_module_register`) against the Node `module` builtin in BOTH source forms (`node:module` AND bare `module`). Four shapes engage: (1) named import + literal call (`import { register } from 'node:module'; register('pkg', ...)`), (2) aliased local name (`import { register as reg } from 'node:module'; reg('pkg', ...)` resolves the local binding through the imports table), (3) namespace-call form (`import * as Module from 'node:module'; Module.register('pkg', ...)` matches a `StaticMemberExpression` callee whose object is a `Namespace` import of `node:module` / `module`), (4) template-literal first arg (`` register(`tsx/esm`) ``, no-substitution only). Non-string specifiers (`register(varName, ...)`), empty-string specifiers, interpolated template literals, and `register` from any other source are skipped. Routed through the existing `dynamic_imports` resolution pipeline, so bare specifiers like `@swc-node/register/esm` resolve to `ResolveResult::NpmPackage("@swc-node/register")` and credit `package_usage`. See issues #293 (initial fix) and #296 (broadened to namespace + template-literal forms; switched vehicle from `require_calls` to `dynamic_imports`).
- **Vitest auto-mocks**: `vi.mock('./services/api')` synthesizes a dynamic namespace import of `./services/__mocks__/api`, crediting the auto-mock sibling file (and all its exports) as used so vitest's `__mocks__/<file>` convention does not surface as `unused-file`. The visitor ALSO records the `vi.mock` target itself as a side-effect dynamic import so a test file that mocks a module (with or without a factory) does not flag the target as `unused-file`, while unused exports in that target remain reportable (issue #311). Handles string-literal sources, expressionless template literals, and the `vi.mock(import('./api'))` argument form. Skipped when the source is empty, contains a URL scheme, starts with `data:`, already contains a `__mocks__` segment (avoids `__mocks__/__mocks__/...`), or is bare-package only with no `/` separator. Path-alias prefixes survive: `vi.mock('@/src/foo/bar')` synthesizes `@/src/foo/__mocks__/bar`, which then resolves through the importer's tsconfig path aliases. **Factory form (`vi.mock(spec, factory)`)**: vitest does NOT consult the `__mocks__/<file>` sibling when a factory is provided, so fallow suppresses the synthesis whenever the second argument is an arrow function, function expression, or any parenthesized variant of either; the target itself is still credited as side-effect reachability. Options-form `vi.mock(spec, { spy: true })` (object literal as second argument) is treated as auto-mock-with-options and DOES still synthesize, matching vitest's runtime behavior (issue #311). The `vi` callee is matched by name without a vitest-import gate (parallel to how `require` is matched); false positives outside test files are inert because the synthesized specifier just fails to resolve, no exports are fabricated and no member accesses are added. **No-sibling case** (issue #378): vitest's auto-mock works in-memory and does not require a `__mocks__/<file>` directory the way Jest does, so the synthesised dynamic import is marked `is_speculative: true` on `DynamicImportInfo`. At resolution time, `resolve_single_dynamic_import` drops speculative entries silently when their target is `Unresolvable`; the credit path (target exists) is unchanged. Without the drop, projects that rely on Vitest's in-memory auto-mocking surface a spurious `unresolved-import` finding pointing at a path they never wrote. Bare-package mocks (`vi.mock('axios')` paired with a project-root `__mocks__/axios.ts`) and Jest's `jest.mock(...)` are intentionally out of scope. See issues #251, #311, #378.
- **Unused import bindings**: via `oxc_semantic` scope-aware symbol analysis. Dead imports don't count as references, improving unused-export precision.
- **TypeScript overload dedup**: `export function foo(): void; export function foo(x: string): string;` treated as single export
- **Import-then-reexport pattern**: `import { X } from './a'; export { X };` is detected as a re-export (semantically equivalent to `export { X } from './a';`) instead of a local export. Without this, the bottom export collides with the original in duplicate-export detection and is never reached by re-export chain propagation. Handles renames on either side (`import { X as Foo }; export { Foo }` or `import { X }; export { X as Y }`), default imports (re-exported with `imported_name: "default"`), and mixed local + re-export in one statement. Source-order-independent (PR #427): `export { X }; import { X } from './a';` (export before import) is also recognized via a pending-specifier finalize step that resolves the `export { X }` against this module's imports and local declarations after the full AST walk. A local declaration named `X` in the same module shadows the import and keeps the export local. Class-inheritance and interface-implementer propagation in `find_unused_members` walks `walk_re_export_origins` for both the parent and the interface key, so `extends BaseShape` / `implements RenderableShape` resolved through a barrel re-exports the parent's `this.*` accesses to the actual defining file. Namespace imports (`import * as ns; export { ns }`) stay as local exports.
- **Class instance members**: `const svc = new MyService(); svc.greet()` and `this.service = new MyService(); this.service.doWork()` track the method as used. Chained `this.field` access uses synthetic `"this.field"` keys in `instance_binding_names`. Factory initializers through wrapper calls are also tracked: `const [svc] = useState(() => new MyService())` binds the first array-destructured element as an instance. Typed parameter / property chains through nested object types are also tracked: `constructor(private deps: { foo: FooClass }) { ... } this.deps.foo.foo()` registers `this.deps.foo -> FooClass` via `extract_nested_type_bindings` walking the `TSTypeLiteral`, then `static_member_object_name` records the full dotted-path access (`this.deps.foo`) which resolves through `binding_target_names` so `FooClass.foo` is credited. Same propagation runs through explicit `this.deps = deps` assignments via `copy_nested_binding_targets`, covering the no-accessibility-modifier form. Angular 14+ field-initializer DI (`private readonly inner = inject(InnerService)`) is recognized: `extract_angular_inject_target` reads the call's `inject<T>()` type argument or first identifier argument and registers `this.inner -> InnerService` so `this.inner.member` chains credit `InnerService.member`. The `inject` callee is gated by a named-import check against `@angular/core`, so a same-named `inject` from any other module is intentionally ignored (false positives over false negatives). **Static-factory call results** (issue #346): when a static method's body's last top-level statement is `return new this()` or `return new <SameClassName>()`, `extract_class_members` flags the matching `MemberInfo` with `is_instance_returning_static`. The visitor records every `const <local> = <ID>.<method>()` shape into a `FactoryCallCandidate` list, and a finalize step resolves each candidate against this module's `local_class_exports` (same-file: bind `local -> ClassName` directly so `resolve_bound_member_accesses` re-emits accesses against the class) or `imports` (cross-file: bind `local -> FACTORY_CALL_SENTINEL:ID:method`). The analyze-layer `propagate_factory_call_accesses` pass parses the sentinel out of `access.object`, resolves `ID` via `build_local_to_export_keys`, walks `walk_re_export_origins` for barrel chains, and credits `access.member` on the origin class export only when its `MemberInfo` for `method` has `is_instance_returning_static`. Conservative: chained `.build()`, ternary returns, and named-target factories (`Wrapper.getBuilder() { return new MyClass(); }`) stay out of scope. Globals like `Math.floor(...)` match neither a local class nor an import and are dropped without effect. Scope-unaware, false matches produce false negatives, not false positives. See issues #244 (Angular `inject()` field), #246 (Playwright fixtures, see next entry), #346 (static-factory methods).
- **Typed instance chains** (issues #386, #388): `ClassHeritageInfo.instance_bindings` are extracted for all classes, not only Angular-decorated classes. The binding set includes non-private typed constructor parameters, typed properties, nested object-type properties, and typed getters, so a non-Angular chain like `factory.service.query()` credits `Service.query` when `factory` is a `Factory` instance and `Factory.service` is declared as `Service`. Reflective whole-object uses such as `Object.keys(factory.service)` propagate through the same typed chain and mark the target class as whole-object used. This covers getter-backed Playwright fixture teardown factories without suppressing genuinely unused service methods. **Generic-constraint substitution** (#388): on `visit_class` enter, the visitor pushes a `<TParam, Option<Constraint>>` map collected via `collect_class_type_param_constraints` onto `class_type_param_constraints` (popped on exit). `record_typed_binding` consults the top of the stack: a constrained parameter substitutes its constraint (`class BaseService<TClient extends BaseClient>` with `constructor(client: TClient)` registers `this.client -> BaseClient`); an unconstrained parameter drops the binding (no resolvable class). The substitution flows through `extract_nested_type_bindings` so nested object-type properties typed as a generic parameter also resolve correctly. `extract_class_instance_bindings` applies the same substitution when populating `ClassHeritageInfo.instance_bindings`.
- **Fluent-builder chain credit** (issue #387): static-builder chains like `EventBuilder.create().setX(1).setY(2).build()` credit each chained method on the originating class. `MemberInfo.is_self_returning` flags instance methods whose declared return type matches the enclosing class name OR whose body's last statement is `return this`. `is_instance_returning_static` widened to also cover static methods whose declared return type matches the class name (covers `static createWithDefaults(): EventBuilder { return chain; }` where the body is too complex to inspect syntactically). At extract time the visitor records one `FLUENT_CHAIN_SENTINEL` `MemberAccess` per call expression chained off a previous call, encoded as `{sentinel}{root_local}:{root_method}:{chain_prefix}` with `member` set to the method being called now; `chain_prefix` is the comma-separated list of intermediate methods walked since the root (empty when the credited member is the first chained call). The analyze-layer `propagate_fluent_chain_accesses` pass resolves the root through each consumer's `local_to_export_keys`, walks each origin via `walk_re_export_origins`, validates the root method has `is_instance_returning_static` and every intermediate chain step is `is_self_returning`, then credits `access.member`. Chains stop at non-self-returning methods, so `.build().toString()` correctly does NOT credit `toString` on the builder. Conservative on mismatch: imports that turn out not to name a class drop silently. Cache version bumped 80 to 81.
- **Playwright POM fixtures**: typed `base.extend<MyFixtures>({ ... })` definitions are correlated with their callback-side member uses so POM class methods consumed only through fixtures are credited. The visitor records two synthetic-object-prefix `MemberAccess` flavors keyed by the test's local name: `__fallow_playwright_fixture_def__:test:fixture` (member = the declared POM type name) and `__fallow_playwright_fixture_use__:test:fixture` (member = the accessed method name). Definitions accept the fixture map as a named type alias (`type MyFixtures = { adminPage: AdminPage }; base.extend<MyFixtures>(...)`), an inline type literal (`base.extend<{ adminPage: AdminPage }>(...)`), an intersection of either, or any parenthesized form; alias bodies are pre-collected into `playwright_fixture_types` so a referenced alias resolves to the same `(fixture, type_name)` pairs as an inline literal. The `base.extend` callee is gated against an `@playwright/test` named-import (`base` aliased from `test`), so `someOther.extend<T>(...)` is ignored. Use-side collection runs at every call expression: when the callee root identifier resolves to a Playwright test object, the first arrow / function argument's object-pattern parameter (`{ adminPage }`, with rename support `{ adminPage: alias }`) seeds a per-call `fixture_by_local` map, and a sub-visit over the callback body emits one use-sentinel per `<local>.<method>` static-member access. Nested fixture types (`{ pages: { adminPage: AdminPage; userPage: UserPage } }`) and nested usage are both supported: type traversal recurses through `TSTypeLiteral` / aliased `TSTypeReference` properties under a dotted path so the def-side sentinel keys become `pages.adminPage`, etc. Use-side handles two consumption styles with the same dotted-path key: nested destructure (`{ pages: { adminPage } }` → `adminPage.method()`) maps the local to its full path, and chained access on a top-level destructure (`{ pages }` → `pages.adminPage.method()`) walks the static-member chain and concatenates the leftmost local's resolved path with the remaining segments. The matched chain is not re-walked, so intermediate member exprs do not emit spurious `(pages, adminPage)` accesses. See issue #268. **Helper-function fixtures** (issue #491): when `base.extend<T>(...)` is returned from a helper instead of bound directly to a `const`, the same sentinel correlation runs keyed by the helper's name. Three declarator shapes capture the helper: `function appTest() { return base.extend<T>(...); }`, `const appTest = () => base.extend<T>(...)`, and `const appTest = function () { return base.extend<T>(...); }`. The body must be a single `return <call>` statement (multi-statement bodies and conditional returns stay out of scope to keep matching conservative). A finalize-time pass (`ModuleInfoExtractor::resolve_playwright_factory_call_definitions`) gates each captured helper on `base` resolving to a `test` named import from `@playwright/test` and emits the def-sentinel `__fallow_playwright_fixture_def__:appTest:<fixture>` per type binding. Same-file helper chains (`function appTest() { return setupTestFixture(); } function setupTestFixture() { return base.extend<T>(...); }`) propagate via a capped fixed-point pass over `(caller, callee)` aliases captured at the same site, so the outer name inherits the inner helper's bindings. Cross-file chains are intentionally out of scope: the propagation is per-module. Use-side recognition extends `playwright_test_callee_name` with a `CallExpression` arm that recurses into the inner call's callee, so `appTest()(...)` and `appTest(opts)(...)` extract the helper name on the def side. Safe because def sentinels gate the analyzer; use sentinels for unmatched helper names produce no credit. The analyzer (`propagate_playwright_fixture_accesses` in `unused_members.rs`) walks both sentinel flavors, resolves the test identifier through each importing file's `local_to_export_keys` so cross-file imports / aliased imports (`import { test as fixtureTest } from './fixtures'`) match the same export key on both sides, follows re-export chains for the POM type via `walk_re_export_origins`, and credits the accessed method into `accessed_members` on the defining file's class export. Runs BEFORE `propagate_accesses_through_re_exports` so barrel-re-exported POMs are also covered.
- **Type-level member access**: `TSQualifiedName` (e.g., `type X = Status.Active`) tracked as member access. Mapped type constraints (`{ [K in Enum]: ... }`, `{ [K in keyof typeof Enum]: ... }`) and `Record<Enum, T>` mark all enum members as used via whole-object use.
- **TypeScript namespace exports**: `export namespace Foo { export function bar() {} }` extracts `Foo` as a single export with inner declarations as `NamespaceMember` entries, not as separate top-level exports. Runtime namespaces (no `declare`) are NOT type-only. `declare namespace`/`declare module` remain type-only. Nested namespaces flatten members into the outermost namespace.
- **Lit / Web Components registration** (knip #1394): classes registered through a runtime side effect at module load are credited as used even when no other file imports the class identifier. Two patterns are recognized: (1) Lit `@customElement('tag')` decorator on a class declaration, in both named-import (`import { customElement }`, including aliases) and namespace-call (`@decorators.customElement('tag')`) forms, only after the decorator binding is verified to come from `lit/decorators.js` or `lit/decorators/custom-element.js`; (2) `customElements.define('tag', ClassRef)` call expressions at any depth where the second argument is an `Identifier`. The visitor records the local class name into `ModuleInfoExtractor.side_effect_registered_class_names` and a post-walk finalizer (`apply_side_effect_registrations`) flips `ExportInfo.is_side_effect_used = true` for every export whose `local_name` is in the set, covering `export class X {}`, `class X {}; export { X }`, and `export default X` patterns. Anonymous `export default @customElement('x') class extends LitElement {}` is also covered by tracking the pending Default export slot and validating the decorator import after the full walk. The graph layer copies the flag to `ExportSymbol.is_side_effect_used`; `find_unused_exports` and `find_unused_members` OR it with `!references.is_empty()` so reachable side-effect-used exports survive both unused-export and skip-whole-export-dead checks. Persisted across cache rounds via `CachedExport.is_side_effect_used`. Lit lifecycle members (`render`, `updated`, etc.) are allowlisted by the new `lit` plugin via heritage-scoped `UsedClassMemberRule::Scoped` rules keyed on `LitElement` / `ReactiveElement`; native Custom Elements lifecycle members (`connectedCallback`, `observedAttributes`, etc.) are built-in heritage-scoped allowlists for `HTMLElement` subclasses so they work without a Lit dependency. Non-lifecycle methods on Web Component classes are still reported.
- **Angular component metadata**: Full `@Component`/`@Directive` decorator metadata extraction prevents `unused-class-members` false positives. External templates (`templateUrl`) emit `SideEffect` imports and scan HTML for Angular syntax. Inline templates (`template:` string/template literals) are scanned in-place. `host:` object binding expressions extract referenced member identifiers. `inputs:`/`outputs:` string arrays mark listed members as framework-managed (handles `'name: alias'` syntax). Angular 17+ signal APIs (`input()`, `input.required()`, `output()`, `outputFromObservable()`, `model()`, `viewChild()`, `viewChildren()`, `contentChild()`, `contentChildren()`) set `has_decorator` on the property when the class has an Angular decorator. All template scanning supports `{{ }}`, `[prop]`, `(event)`, `[(ngModel)]`, `*ngIf`, `*ngFor`, `@if`, `@else if`, `@for`, `@switch`, `@defer (when expr)`, `@let name = expr;`, pipes. `*ngFor` and `@for` bindings are tracked as locals. `@let` variables are added as template-local bindings. `@defer` `when`/`prefetch when`/`hydrate when` conditions are scanned for member refs. **Inherited members and DI-injected service access** (issue #174): child-component template refs propagate up through `extends` chains so a member only defined on a base class is credited when referenced from a child's template. Member-access chains on unresolved identifiers (`dataService.getTotal()` in the template) are resolved through the importing component's typed instance bindings - constructor parameters with accessibility modifiers (`constructor(public dataService: DataService)`) and non-private typed property declarations are captured into `ClassHeritageInfo.instance_bindings` and used at analysis time to credit the target class's member as used. Inline templates reuse the visitor's existing `binding_target_names` resolver, external templates resolve in the analysis bridge.

## Resolution-level
- **Package.json `exports` subpath**: output dirs (dist/build/out/esm/cjs) mapped back to src/ with source extension fallback, including nested subdirs
- **Pnpm virtual store**: `.pnpm` paths mapped back to workspace source files. Handles injected deps, scoped/unscoped packages, peer dependency suffixes.
- **Package.json `imports` (`#subpath`)**: simple mappings, wildcard patterns, conditional exports. Per-package scoping.
- **Export / import condition names**: `oxc_resolver` is configured with the baseline condition set `["development", "import", "require", "default", "types", "node"]`. `"development"` ships unconditionally so packages that declare a `development` branch in `exports` / `imports` (common pattern: source files for dev, compiled output for prod; used by Vite, Vitest, esbuild, Rollup) resolve deterministically to source. When the React Native or Expo plugin is active, `"react-native"` and `"browser"` are prepended. Users extend the list via the `resolve.conditions` config key; those entries prepend ahead of everything else. See issue #135 and <https://nodejs.org/api/packages.html#community-conditions-definitions>.
- **React Native platform extensions**: `.web.ts`, `.ios.ts`, `.android.ts`, `.native.ts` resolved alongside standard extensions
- **Web-root-relative paths**: `/src/main.tsx` or `/static/style.css` resolved as a web convention (Vite/Parcel/Hono). Converts to `./src/main.tsx` and resolves from the source file's parent directory first, then falls back to project root. This handles monorepo workspace members where `site/index.html` referencing `/src/main.tsx` resolves to `site/src/main.tsx`, not `<root>/src/main.tsx`. Applied to HTML, JSX, TSX, JS/TS, and Glimmer source files (extensions `.html`, `.jsx`, `.tsx`, `.js`, `.ts`, `.mjs`, `.cjs`, `.mts`, `.cts`, `.gts`, `.gjs`). JSX/TSX support is needed for SSR frameworks like Hono where layout components emit `<link href="/static/style.css" />` from JSX templates. See issue #105 (till's comment).
- **CSS scoped package imports**: CSS `@import` with scoped npm packages (`@fontsource/monaspace-neon/400.css`, `@company/design-system/styles.css`) are kept as bare specifiers for node_modules resolution. Unscoped bare imports with CSS/SCSS extensions are normalized to relative paths (`app.css` becomes `./app.css`).
- **Tailwind CSS `@plugin` directives**: CSS/SCSS `@plugin "pkg"` targets are extracted as default imports so package dependencies are credited and relative plugin modules have their default export marked used. Unlike SCSS `@use`, extensionless package names such as `daisyui` stay bare even in `.scss`/`.sass` files; otherwise they would be misread as local partials.
- **Tsconfig path aliases**: per-file discovery resolves `@/utils` by finding nearest tsconfig.json per file
- **Broken tsconfig chain fallback**: when `resolve_file` fails with a tsconfig-loading error (broken `extends` chain, circular extend, self-referencing project reference, malformed JSON, IO error), fall back to `resolve(dir, specifier)` which bypasses `TsconfigDiscovery::Auto`. A solution-style `tsconfig.json` with a broken sibling reference no longer poisons relative/bare-import resolution for files covered by a healthy sibling. A single `tracing::warn!` per unique error surfaces the broken chain to the user. Path aliases in the broken workspace remain unresolvable (they were already broken in `tsc`). See issue #97.
- **SCSS partial resolution**: `@use 'variables'` and `@import '../styles/variables'` resolve to `_variables.scss` per SCSS convention. Extensionless bare specifiers in `.scss`/`.sass` files (excluding `sass:*` built-ins) are normalized to relative paths at extraction time. Resolution fallback tries: (1) `_filename` partial prefix, (2) `specifier/_index` directory index, (3) `specifier/index` plain index. Prevents false unresolved-imports, unlisted-dependencies, and unused-files from SCSS partial references.
- **SCSS include paths**: framework plugins contribute absolute directories to `PluginResult.scss_include_paths`. The Angular plugin reads `projects.*.architect.build.options.stylePreprocessorOptions.includePaths` from `angular.json`; the Nx plugin reads the same key from `project.json`. Nx workspace tokens (`{projectRoot}`, `{workspaceRoot}`) in include paths are expanded before resolution: `{projectRoot}` is computed from the config file's parent relative to the workspace root, `{workspaceRoot}` is stripped (paths are already resolved from workspace root). See issues #103, #114. Bare SCSS specifiers that fail the file-local `try_scss_partial_fallback` retry against each include dir, applying `<name>`, `_<name>`, `<name>/_index`, `<name>/index` candidates. Only triggers for `.scss`/`.sass` importers and normalized bare specifiers (those prefixed with `./` at extraction time) — explicit parent-relative paths like `../shared/vars` are left alone.
- **SCSS node_modules resolution**: bare SCSS/Sass specifiers that fail both the local-partial and includePaths fallbacks are retried against `node_modules/` by walking up from the importing file to the filesystem root (matching Node.js / Sass semantics). `try_scss_node_modules_fallback` probes `<nm>/<pkg>/<path>.{scss,sass,css}`, `<nm>/<pkg>/<parent>/_<name>.{scss,sass}` (partial convention), and `<nm>/<pkg>/<path>/{_index,index}.{scss,sass}` (directory index). The CSS-extension append handles Sass's fallback for files like `animate.css/animate.min` → `animate.css/animate.min.css`. Because `node_modules/` files are outside fallow's file index (excluded by default ignore patterns), candidates are probed via `is_file()` on disk and a successful hit returns `ResolveResult::NpmPackage` (extracted via `extract_package_name_from_node_modules_path`), keeping `unused-dependencies` / `unlisted-dependencies` accurate. Scoped packages (`@scope/pkg/path`) are not prefixed with `./` by `normalize_css_import_path`, so they flow through `oxc_resolver`'s bare-module path directly and never reach this fallback. See issue #125.
- **SCSS importer JS/TS rejection**: when the importing file is `.scss` / `.sass` AND the standard resolver returns a path with a JS/TS-family extension (`.tsx`, `.ts`, `.mts`, `.cts`, `.gts`, `.js`, `.jsx`, `.mjs`, `.cjs`, `.gjs`), the result is rejected and resolution re-routes through the SCSS-aware fallback chain (CSS-extension probe, partial convention, include paths, node_modules). When those also fail, the import is reported as `Unresolvable` rather than falling through to the JS/TS extension list. Sass's resolution algorithm only ever considers `.css` / `.scss` / `.sass` files, so a sibling `.tsx` cannot legally satisfy a Sass `@use` / `@import` even though `oxc_resolver`'s extension list places `.tsx` before `.scss`. Without this guard, `@use 'Widget'` from a `.scss` file resolved to `Widget.tsx` whenever both files existed, creating phantom 3-file circular dependencies in standard CSS-modules / Angular `styleUrls` patterns where `.tsx` components import their own `.scss` and a sibling `.scss` shares variables/mixins via `@use`. See issue #245.
- **Entry point source fallback for ignored output dirs**: when `package.json` `main`/`module`/`exports` resolves inside an output directory (`dist`, `build`, `out`, `esm`, `cjs`) and the direct `src/` mirror in `try_output_to_source_path` does not exist, `resolve_entry_path` probes `src/index.*`, `src/main.*`, `index.*`, `main.*` in the package root. Prevents the entire `src/` tree from becoming unreachable in libraries whose compiled entry (e.g. `dist/esm2022/index.js`) has no one-to-one source mirror. Emits a single `tracing::info!` per fallback. See issue #102.
- **Workspace package self-reference & cross-workspace fallback**: when a bare specifier's package name matches a workspace package and normal resolution either fails or lands inside `node_modules` for that same package, `try_workspace_package_fallback` strips the package name prefix and resolves the subpath (`button`, `internal/base`, etc.) against a synthetic importer inside the workspace root. Uses `oxc_resolver`'s `resolve_file` so directory indices, source extensions, and the workspace's own `tsconfig.json` aliases still apply. Handles both Node.js v12+ self-referencing (`import '@org/pkg/sub'` from inside `@org/pkg`, common in Angular libraries built with `ng-packagr`) and cross-workspace imports in monorepos that haven't run `npm install`. See issue #106.

## Graph-level
- **Type-only cycle filtering**: `import type` edges carry `is_type_only` through `ImportedSymbol` to cycle detection. Edges where all symbols are type-only are excluded from Tarjan's SCC successor list, preventing false circular dependency reports from type-only bidirectional imports.
- **`export *` chain propagation**: multi-level barrel file chains fully resolved for transitive usage tracking. The fixpoint loop in `crates/graph/src/graph/re_exports/mod.rs::resolve_re_export_chains` terminates naturally via the monotone dedup-by-`from_file` check in `propagate.rs`; a defensive safety backstop bounded by `re_export_info.len() + 1` iterations guards against future regressions. Chain depth is unbounded in practice (the old `max_iterations = 20` cap silently truncated barrels with deeper chains; removed for #442). Re-export cycles are detected up front via iterative Tarjan SCC over the (barrel, source) edge subgraph; cycles surface BOTH as one `tracing::warn!` per cycle naming the member paths (RUST_LOG=warn discoverability, unchanged from #442) AND as a user-visible `re-export-cycle` finding on `AnalysisResults.re_export_cycles` (#515). Self-re-exports (a barrel re-exporting from itself, often a rename leftover) carry `kind: "self-loop"`; multi-node SCCs carry `kind: "multi-node"`. The finding is file-scoped (no per-edge line/col anchor); suppression via `// fallow-ignore-file re-export-cycle` (or aliases `re-export-cycles` / `reexport-cycle` / `reexport-cycles`) on any member file drops the cycle from results, and per-file `overrides.rules.re-export-cycle` is a no-op because the cycle spans multiple files (resolver emits a load-time warn pointing at the working escape hatch). Synthetic bridge stubs created during multi-hop star propagation inherit `is_type_only` from the triggering re-export edge so multi-hop `export type *` chains tag the stub correctly; when both a type-only and a value star edge reach the same source for the same name, a `synthetic_stubs` side set tracks the stub identity so the value edge can downgrade `is_type_only: false`. Real `export type Foo` declarations on the source are never in the side set and stay type-only. Type-only re-export cycles (`export type * from './b'` paired symmetrically) still surface as findings, because chain propagation through the loop is the same no-op as for value cycles. See `decisions/005-re-export-chain-resolution.md`.
- **`export * as Foo from './bar'` namespace re-export propagation**: a barrel that namespace-re-exports a module records a `ReExportEdge { imported_name: "*", exported_name: "Foo" }` plus a synthesised stub `ExportSymbol` named `Foo` on the barrel. The Phase 2 narrowing path only handles `import * as ns` (direct namespace imports), and Phase 4's named-re-export propagation looks for a source export literally named `"*"` (never matches), so a consumer using the named import form (`import { Foo } from './barrel'; Foo.X`) would otherwise leave every export of `./bar` unreferenced. Phase 2c (`graph::namespace_re_exports::propagate_namespace_re_exports`) closes the gap: it iterates re-export edges with `imported_name == "*" && exported_name != "*"`, enumerates the reachable `(barrel_file, exported_name_at_barrel)` pairs via a forward BFS over named re-export edges (so multi-hop chains like `outer_barrel.Foo -> inner_barrel.Foo -> source` match), and for every consumer that imports a reachable name records each `<consumer_local>.<member>` access. Credits are applied with `mark_member_exports_referenced` plus `create_synthetic_exports_for_star_re_exports` on the namespace target file, mirroring `narrow_namespace_references`'s two-step shape so star barrels on the target (`./source/index.ts` doing `export * from './impl'`) get a synthetic stub the Phase 4 chain resolver carries through. Whole-object uses (`Object.values(Foo)`, spread, destructure-with-rest) credit every target export; entry-point barrels exposing the namespace externally also credit every target export, mirroring `propagate_entry_point_star`. Consumer bindings reported as unused (`unused_import_bindings`) are skipped. The walker intentionally does NOT follow `export * as ns from` edges forward at non-seed barrels because the original identifier is hidden behind `ns.<name>` rather than exposed directly. See issue #324.
- **CSS Modules**: default imports (`import styles from '...'`) resolve `styles.className` to named exports via graph-level narrowing. Spread/`Object.values` handled conservatively.

## Analysis-level
- **tap and tsd test entry plugins**: the tap plugin activates from `tap` and marks node-tap default test discovery (`test/`, `tests/`, `__tests__/`, `.test.` / `.spec.` files, and top-level `test.*` / `tests.*`) as `EntryPointRole::Test`. The tsd plugin activates from `tsd`, marks `.test-d.ts(x)` declaration tests as test entry points, and reads `package.json#tsd.directory` so custom directories such as `test/types` stay reachable.
- **Duplicate export common-importer filter**: duplicate exports are only reported when at least two files sharing the same export name also share a common importer in the module graph. Unrelated leaf files (e.g., SvelteKit route modules in different directories) that coincidentally export the same name are not flagged.
- **Duplicate-exports honors `ignoreExports`**: a file matched by a config `ignoreExports` rule is excluded from duplicate-exports grouping (`exports: ["*"]` excludes the whole file, a name list excludes only those names). Built for shadcn / Radix / bits-ui-style component libraries where many `components/ui/<name>/index.ts` barrels intentionally export the same short names (`Root`, `Content`, `Trigger`). Plugin / framework `used_exports` rules are NOT consulted for duplicate-exports gating: they say "this export is framework-managed", not "this export's duplication across barrels is intentional". Per-file `overrides.rules.duplicate-exports` is a no-op because the rule's severity is project-wide and the finding spans multiple files; the resolver emits a load-time `tracing::warn` for any `overrides.rules.duplicate-exports` or `overrides.rules.circular-dependency` setting pointing users at the working escape hatch. See issue #317.
- **Decorated class members**: members with decorators (NestJS `@Get()`, Angular `@Input()`, TypeORM `@Column()`) default to NOT being reported unused, because frameworks consume them reflectively. The `MemberInfo.decorator_names: Vec<String>` field captures the FULL dotted path of each decorator (`@step("x")` -> `"step"`; `@ns.foo` -> `"ns.foo"`). The skip predicate in `find_unused_members` consults `FallowConfig.ignore_decorators`: a method whose every decorator path is in the ignore set is checked normally; a method with any non-ignored decorator stays skipped. Default empty list preserves the legacy skip-all behavior. Matching rule: dotted entries match the full path; bare entries match the leftmost segment, so `"decorators"` collapses every `@decorators.*` decorator. Both `"@step"` and `"step"` accepted at config load (leading `@` stripped). Angular signal-initializer properties (`input()`, `output()`, etc.) carry `has_decorator: true` with empty `decorator_names` and always skip; the gate has no name to match against the ignore list. Unmatched ignore-list entries emit a one-time `tracing::warn!` at end of run via `IgnoreDecoratorSet::warn_unmatched`, mirroring `ClassMemberAllowlist::warn_unmatched_patterns`. The gate operates on the DECLARING class's `decorator_names`; inherited members carry their own (or no) decorators. See issue #471.
- **Configurable class member allowlist**: the built-in Angular/React lifecycle check (see `predicates::lifecycle`) is extended at analysis time with two user-controlled sources: (1) top-level `FallowConfig.used_class_members`, (2) `Plugin::used_class_members()` / `ExternalPluginDef.used_class_members` on active plugins. Both sources feed `AggregatedPluginResult.used_class_members`, which `analyze/mod.rs` merges with the config list into a single `FxHashSet<&str>` passed to `find_unused_members`. Intended for third-party interface patterns where a library calls consumer methods reflectively (ag-Grid's `agInit`/`refresh`, Web Components' `connectedCallback`). The allowlist is scoped to class members — matching enum member names are still flagged. Member strings containing `*` or `?` compile as `globset::Glob` matchers, supporting `"*"` (every member on a heritage-matching class), `"enter*"` / `"*Handler"` (prefix / suffix), and `"on*Event"` (combined). Globs run alongside exact-name matches and respect any `extends` / `implements` heritage clause on a scoped rule. Glob patterns matching zero members emit a single `WARN` per pattern at the end of the run via `ClassMemberAllowlist::warn_unmatched_patterns`, surfacing dead allowlist entries; exact-name entries do not warn. See issues #98, #254.
- **Cross-package namespace re-export through object literal**: `import * as foo from './bar'; export const API = { foo }` in a barrel followed by `import { API } from '@scope/lib'; API.foo.bar` in another package credits `bar` on `./bar.ts`. The visitor records per-module `NamespaceObjectAlias` entries `{ via_export_name, suffix, namespace_local }` at finalize time (covering shorthand `{ foo }` and nested forms `{ motionNet: { adEngine } }`); both `into_module_info` and the SFC `merge_into` paths populate them. The graph layer threads them onto `ResolvedModule` and a Phase 2b pass (after reference population, before reachability) walks each consumer's `member_accesses` for `object == "<consumer_local>.<suffix>"`, looks up the namespace import's target file via the module's resolved imports filtered to `ImportedName::Namespace`, and runs the `mark_member_exports_referenced` + `create_synthetic_exports_for_star_re_exports` pair from `narrowing.rs` so star-barrel namespace targets (`./foo/index.ts` doing `export * from './bar'`) get a synthetic stub export the Phase 4 chain resolver can carry to the real defining file. Default-export aliases match against `ImportedName::Default`. **Consumer-side multi-hop** (issue #310): the alias-defining file may sit behind 1-3 named-re-export barrels (`pkg/index.ts` -> `pkg/methods/index.ts` -> `pkg/methods/methods.ts`); the matcher does a forward BFS over re-export edges starting from `(alias_file, via_export_name)` to enumerate every `(barrel_file, exported_name_at_barrel)` pair through which the alias is reachable, then matches consumer imports against the full set. Plain `export *` propagates names unchanged; `export { A as B }` propagates the renamed `B`; `export * as ns` is skipped at non-seed barrels (its source identifiers are hidden behind `ns.<name>`, not exposed directly). **Target-side multi-hop** (issue #328): the namespace target itself may chain through `export * as N from './source'` namespace re-exports (`barrel.ts` does `export * as inner from './leaf'`; consumer accesses `API.foo.inner.X`). After the initial Phase 2b credit lands on a name namespace-re-exported on the target, the matcher walks chained `export * as <name> from './source'` edges on the alias-target side and propagates the consumer's deeper accesses (`<expected_object>.<member>.<X>`) onto the underlying source recursively via `ChainWalkCtx`. Cycle-protected by a per-access visited set keyed on module index. Bumps the extract `CACHE_VERSION` because the new `namespace_object_aliases` field changes serialized module state. See issue #303 (cross-package case) building on issue #269 (same-file case which `resolve_bound_member_accesses` already handled by re-emitting the access against the namespace local); #310 (consumer-side multi-hop); #328 (target-side namespace-re-export chain).
- **Cross-package member access through barrels**: `import { Foo } from '@scope/lib'` followed by `Foo.bar` correctly credits `bar` on the originating file even when `@scope/lib`'s entry resolves to a barrel that re-exports `Foo` from a sibling module. Phase 4 chain resolution synthesizes a stub `ExportSymbol` on the barrel for reference tracking, so without further propagation the access map keys at the barrel and the detection loop's origin-keyed lookup misses where `members` are populated. `find_unused_members` walks each access key through `ReExportEdge` chains (named re-exports, renamed re-exports, and `export *` fan-out when no named edge matches) to every defining-site export and copies the access set. Cycle-protected. `export * as ns from './mod'` edges are intentionally skipped because two-level `ns.Foo.member` access isn't tracked at extract time. Same propagation runs for `whole_object_used_exports` (`Object.values(Foo)` on a re-exported enum). Covers both `unused_enum_members` and `unused_class_members`. See issue #178.
- **Inheritance-aware class members**: `extends` clauses are extracted to build an inheritance map. Parent class `this.*` accesses propagate to child class overrides (e.g., `BaseShape.describe()` calling `this.getArea()` credits `Circle.getArea()`). External member accesses propagate bidirectionally through the hierarchy. `super.method()` calls inside a subclass body are attributed to the parent via a `class_super_stack` in the visitor: the super-class identifier from the `extends` clause is pushed on class entry and popped on exit, and `Super.member` expressions record a `MemberAccess { object: <super_local>, member }` which `local_to_imported` then resolves to the parent's export. This covers the common pattern of a base class method only called via `super.method()` from subclasses. Only simple identifier extends are tracked (`class Foo extends Bar`), not mixin patterns. Methods called through parent-typed parameters remain unattributed (requires type inference, out of scope per ADR-001). See issue #130.
- **Private type leaks**: an exported value, function, or class whose public signature references a same-file type that is NOT exported by name is reported as `private-type-leak`. Default severity: `Off` (opt-in API hygiene check). Enable with the `--private-type-leaks` filter for a one-off run, or `private-type-leaks: "warn"` / `"error"` in `rules` for persistent enforcement. The CLI filter promotes the rule from `Off` to `Warn` for that invocation only via `IssueFilters::activate_explicit_opt_ins` in `crates/cli/src/check/mod.rs`. Detection runs in `find_private_type_leaks` (`crates/core/src/analyze/unused_exports.rs`) over `module_info.public_signature_type_references` (collected by the visitor's `SignatureTypeCollector`) cross-checked against `module_info.local_type_declarations`. Same-file private = the referenced name is in `local_type_declarations` AND not in the module's `exports`. References from ECMAScript private fields (`#field: T`) and TypeScript `private` accessibility members are NOT collected (the visitor short-circuits in `collect_class_signature_refs`), so they cannot leak. Qualified type names (`Foo.Bar.Baz`) reduce to the root identifier (`Foo`), since the leak is "the root namespace is private", not "every dotted segment is". Storybook story files (`*.stories.{ts,tsx,js,jsx,mts,cts,mjs,cjs}` and `*.story.*` variants) are skipped via `is_storybook_file`: the canonical `type Story = StoryObj<typeof X>; export const Default: Story = ...` pattern would otherwise generate one leak per story across thousands of UI library files. Framework routing convention files are also skipped via `is_route_convention_file` (Next.js App Router page/layout/template/loading/error/not-found/route/default/global-error/forbidden/unauthorized + the metadata files opengraph-image/twitter-image/icon/apple-icon/manifest/sitemap/robots; Next.js Pages Router; Gatsby `templates/`; Remix v2 + TanStack Router top-level + folder-route entries route/_layout/_index/index/__root; Expo Router `_layout` and `+*` files). All patterns prefixed with `**/` so they match in monorepo subpackages (`packages/web/src/app/.../page.tsx`). Real-world delta: amictus.ai 62 → 59 leaks (3 Next.js convention noise eliminated, 59 remaining are real React `Props`-pattern findings that warrant exporting the Props type). Companion logic in `suppress_signature_backing_types` removes any `unused_types` finding whose export is the type name backing a public signature in the same file, so exporting a backing type to fix one leak does not create a new "unused type" finding.
- **JSDoc visibility tags** (`@public`, `@internal`, `@beta`, `@alpha`, `@expected-unused`): exports annotated with any of these JSDoc/TSDoc tags are never reported as unused. Stored as a `VisibilityTag` enum on `ExportInfo` and `ExportSymbol`, preserving the semantic distinction (Public = API surface, Internal = sister packages/build tools, Alpha = early preview, Beta = unstable, ExpectedUnused = intentionally unused). Priority when multiple tags present: Public > Internal > Alpha > Beta > ExpectedUnused. Only `/** */` block JSDoc comments recognized. `@api public` TSDoc variant also supported. Word-boundary checked (e.g., `@internalizer` does not match). Survives incremental cache roundtrip via `u8` discriminant encoding. `@expected-unused` differs from other tags: when the tagged export IS actually used (has references from reachable modules), a stale suppression is reported instead of silently accepting it. This supports the pattern of marking API-completeness exports that become adopted over time.
- **Stale suppression detection**: after all detectors run, `SuppressionContext` compares consumed vs. all suppression comments. Unmatched `// fallow-ignore-next-line` and `// fallow-ignore-file` comments are reported as stale. Default severity: `Warn`. CLI-side suppression kinds (complexity, coverage-gaps, feature-flag, code-duplication) are excluded from stale detection to avoid false positives, since their suppression checks run in the CLI layer, not in `find_dead_code_full`. **Suppressions for disabled rules** (`rules.<kind> == "off"`, including per-file `overrides.rules`) **are NOT reported as stale** (issue #482). `SuppressionContext::find_stale` resolves rules once per file via `ResolvedConfig::resolve_rules_for_path` and skips emission when the suppression's target kind has severity `Off`. The suppression documents intentional dormancy and becomes valid again when the rule is re-enabled. Blanket markers (`// fallow-ignore-next-line` with no kind) are not anchored to any specific dormant kind, so they still surface as stale when nothing matches. The `Suppression` struct stores `comment_line: u32` alongside the target `line: u32` so stale reports point at the comment itself, not the suppressed line. `@expected-unused` JSDoc tags produce stale entries via the unused export detector when the tagged export becomes used. Both comment and JSDoc tag stale entries feed into the same `stale_suppressions` vec on `AnalysisResults` with a discriminated `SuppressionOrigin` enum. **Partial-accept on unknown tokens** (issue #449): when a marker carries a multi-kind list and one token does not parse to any `IssueKind`, the recognized tokens are still recorded as suppressions and the unknown token is preserved on `ModuleInfo.unknown_suppression_kinds`. `SuppressionContext::find_stale` emits one extra `StaleSuppression` per unknown token, reusing `SuppressionOrigin::Comment` with `issue_kind: Some(<verbatim token>)`, `is_file_level` mirrored from the marker, and a new `kind_known: false` field (additive, skip-serialize-if `true`). `StaleSuppression::explanation()` branches on `kind_known`: stale-but-known kinds keep the prior `no <kind> issue found <scope>` copy; unknown tokens render `'<token>' is not a recognized fallow issue kind. Did you mean '<suggestion>'? Other tokens on this line still apply.` where `<suggestion>` comes from `closest_known_kind_name` (Levenshtein distance ≤ 2 against `KNOWN_ISSUE_KIND_NAMES` in `crates/types/src/suppress.rs`, dropped when no close match exists). Cache shape grows a sibling `CachedUnknownSuppressionKind` vec; `CACHE_VERSION` bumped 86 to 87. jq scripts in `action/jq/annotations-check.jq`, `action/jq/summary-check.jq`, and `ci/jq/summary-check.jq` branch on `kind_known == false` to emit unknown-kind annotation copy instead of "no longer matches any active issue."
- **Arrow-wrapped dynamic imports**: `React.lazy(() => import('./Foo'))`, `loadable(() => import('./Component'))`, `defineAsyncComponent(() => import('./View'))`, and any function call with an arrow or function expression argument whose body is a sole `import()` expression. The default export of the target module is credited as used (lazy loading wrappers always consume the default export). Handles expression body (`() => import('./x')`), block body (`() => { return import('./x'); }`), and function expressions (`function() { return import('./x'); }`). The import span is added to `handled_import_spans` to prevent duplicate side-effect edges from the generic `visit_import_expression` walker.
- **Route-callback dynamic imports**: object-literal properties named `component`, `loadChildren`, or `loadComponent` whose value is `() => import('./X')` (or a function-expression equivalent) credit the target module's default export as used, even when no `.then(m => m.default)` is spelled. Covers Angular Router's `loadChildren: () => import('./feature.routes')` and Vue Router's `component: () => import('./View.vue')`, where the framework consumes the module's default export internally. Implemented via `try_extract_property_callback_import` in `crates/extract/src/visitor/mod.rs`. Property names are an exact whitelist; other property names with the same shape (e.g., `loader: () => import('./X')`) are not credited. See issue #253.
- **Dynamic import `.then()` callback**: `import('./lib').then(m => m.foo)` tracks `foo` as a named import from `./lib`. The standard Angular lazy-loading pattern (`loadComponent: () => import('./x').then(m => m.Component)`). Three sub-patterns: (1) expression body member access (`m => m.foo`) extracts specific member names as `destructured_names`, (2) destructured parameter (`({ foo, bar }) => { ... }`) extracts names from the object pattern, (3) identifier parameter with block body (`m => { ... }`) registers as a namespace binding for member-access narrowing. Also handles `({ default: m.Foo })` object literal bodies (React.lazy `.then` pattern) and function expression callbacks. The import span is added to `handled_import_spans` to prevent duplicate side-effect edges. See issue #115.
- **Infrastructure entry points**: Dockerfiles, Procfiles, fly.toml scanned for source file refs. Searches root and config/docker/deploy subdirs.
- **TypeScript project references**: tsconfig.json `references` field discovered as workspaces (additive with npm/pnpm workspaces)
- **CI file scanning**: `.gitlab-ci.yml` and `.github/workflows/*.yml` scanned for binary invocations (npx, direct binaries) AND positional file arguments (`node scripts/deploy.ts`, `--config foo.cjs`). Binary names map to npm packages (CI-only deps stay flagged-as-used); file-path arguments are seeded as project-relative entry patterns so scripts invoked only from CI are not reported as `unused-files` (issue #195 Case D). Line-based YAML parser; known limitations: variable interpolation (`${{ matrix.env }}/deploy.ts`), `\` line-continuations, and YAML anchors (`<<: *defaults`) are silently skipped.
- **Script-referenced hidden directories**: the file walker's hidden-directory filter normally allows only `.storybook` / `.vitepress` / `.well-known` / `.changeset` / `.github`. When a `package.json#scripts` command references a path inside another hidden directory (e.g. `eslint -c .config/eslint.config.js`, `vitest --config .config/vitest.config.ts`, `tsx ./.scripts/build.ts`), `collect_script_hidden_dir_scopes` in `crates/core/src/discover/mod.rs` extracts the hidden segment names and contributes them to the per-package `HiddenDirScope` mechanism so the walker traverses them. Without this, the referenced file is detected as an entry point but never parsed, and its imports become phantom unused dependencies. Three guardrails: (1) only the structured outputs of `crate::scripts::parse_script` (`config_args` + `file_args`) are inspected, so a logging path like `.nx/cache/result.json` mentioned anywhere in a script body cannot pull `.nx/` into scope; (2) `SCRIPT_SCOPE_DENYLIST` excludes known build-cache / VCS / IDE / package-manager state directories (`.git`, `.next`, `.nuxt`, `.output`, `.svelte-kit`, `.turbo`, `.nx`, `.cache`, `.parcel-cache`, `.vercel`, `.netlify`, `.yarn`, `.pnpm-store`, `.docusaurus`, `.vscode`, `.idea`, `.fallow`, `.husky`) regardless of script content; (3) paths containing `..` segments are skipped to keep scopes from escaping a package root. Scopes are per-package: root scripts contribute a scope rooted at `config.root`, workspace scripts contribute scopes rooted at each workspace root. `collect_hidden_dir_scopes` is the combined entry point that loads each workspace's `package.json` once for both plugin-derived and script-derived scopes. A `tracing::debug!` line per inferred scope (`RUST_LOG=fallow_core=debug`) surfaces support diagnostics. See issue #358.
- **Production mode**: excludes test/dev files, only start/build scripts, skips unused devDeps, detects type-only deps. Config file exclusion (`*.config.*`) is root-anchored only; nested `*.config.*` files (e.g., Angular's `src/app/app.config.ts`) are preserved. All production exclude globs use `literal_separator(true)` so `*` cannot cross path separators. See issue #111.
- **Test-only dependency detection**: `find_test_only_dependencies` identifies production dependencies imported exclusively from test/dev/config files (should be devDependencies). Uses a dual classification: glob patterns from `PRODUCTION_EXCLUDE_PATTERNS` for test/story files (`**/*.test.*`, `**/*.spec.*`, `**/__tests__/**`, etc.) and the curated `is_config_file()` predicate for tooling config files at any depth. The predicate approach avoids false matches on application config files like `app.config.ts` while correctly matching workspace-level configs like `packages/foo/vitest.config.ts`. See issues #111, #112.
- **Platform built-in modules**: `node:`, `bun:`, `cloudflare:`, `sass:` prefixed imports and Deno `std` are recognized as builtins — never flagged as unlisted dependencies. The `sass:` prefix covers the Sass compiler's built-in modules (`sass:math`, `sass:string`, `sass:color`, `sass:list`, `sass:map`, `sass:meta`, `sass:selector`) imported via SCSS `@use 'sass:math'`. See issue #104.
- **`ignoreDependencies` dual suppression**: listed packages are excluded from both unused dependency AND unlisted dependency detection, making it useful for runtime-provided packages (e.g., `bun:sqlite`).
- **@types/ unlisted dep suppression**: imports from `pkg` not flagged as unlisted when `@types/pkg` is in deps (TypeScript resolves types from `@types/` and erases the import regardless of `import type` syntax). Scoped packages use DefinitelyTyped convention (`@scope/pkg` → `@types/scope__pkg`).
- **Internal workspace packages treated as first-class deps** (issue #228): a workspace package name (e.g., `@repo/ui`) appearing in another workspace's `dependencies` is checked the same way an external npm package is. `credit_workspace_package_usage` in `crates/core/src/lib.rs` walks resolved imports and re-exports, and for each bare specifier whose resolved target is `ResolveResult::InternalModule` AND whose extracted package name matches a workspace, credits `graph.package_usage` (and `type_only_package_usage` when the import is type-only). Effects: declared-but-unimported workspace deps surface in `unused_dependencies`; imports of a workspace package from a workspace whose own `package.json` does not list it surface in `unlisted_dependencies`. Workspace self-references stay allowed because `find_unlisted_dependencies` inserts each workspace's own name into its dep set before per-file scoping. External plugins also activate per-workspace via `process_external_plugins` inside `run_workspace_fast`, so a workspace dep that enables a custom plugin (`fallow-plugin-*` definitions, inline framework configs) keeps the plugin's `alwaysUsed` and `toolingDependencies` honored for that workspace.
- **Architecture boundary violations**: user-defined zones (glob patterns) with directional import rules. Fires at direct import site using resolved target's zone. Unzoned files are unrestricted in both directions. First-match zone classification. Self-imports always allowed. Inline and file-level suppression supported. Built-in presets: `layered` (presentation/application/domain/infrastructure), `hexagonal` (adapters/ports/domain), `feature-sliced` (app/pages/widgets/features/entities/shared), `bulletproof` (app/features/shared/server, shared zone covers components, hooks, lib, utils, utilities, providers, shared, types, styles, i18n). Presets auto-detect `rootDir` from tsconfig.json for pattern prefix. User zones/rules merge on top of presets (same-name replaces). Zones with zero file matches emit warnings. **`autoDiscover`** (issue #368): a zone declaring `autoDiscover: ["src/features"]` expands at config-resolve time (`expand_auto_discover` in `crates/config/src/config/boundaries.rs`) into one child zone per immediate child directory, named `<parent>/<child>` (e.g. `features/auth`, `features/billing`). Rules referencing the parent (`from: "features"`) expand to each discovered child; explicit child rules (`from: "features/auth"`) override generated parent rules via `dedupe_rules_keep_last` regardless of declaration order. **Bulletproof preset uses autoDiscover for the `features` zone with empty `patterns`** so a top-level `src/features/index.ts` barrel stays unclassified (unrestricted), preventing false-positive `features -> features/<child>` violations when the barrel re-exports children; the trade-off is that other top-level files in `src/features/` (e.g. `features/types.ts`) are also unrestricted. Pinned by `bulletproof_top_level_features_file_is_unrestricted` in `crates/core/tests/integration_test/boundary_violations.rs`. A user-defined zone setting BOTH `patterns` and `autoDiscover` triggers a once-per-process `tracing::warn!` because the parent fallback typically produces false positives on barrels; the warn points at the strict-mode escape hatch. Follow-up #372 tracks differentiated rule expansion that would let the parent rule auto-allow discovered children without re-introducing sibling-feature leaks. **`allowTypeOnly`** (issue #365): a per-rule `Vec<String>` mirroring `allow`'s shape. When `featureB` has `allow: [], allowTypeOnly: ["featureA"]`, type-only imports from `featureB` to `featureA` are admitted: `import type {...}`, `import type * as ns`, inline `import { type Foo }` where every named specifier is type-qualified, AND type-only re-exports `export type { Foo } from "..."`. The predicate is `edge.symbols.iter().all(|s| s.is_type_only)`, identical to cycle detection's exclusion of type-only edges. Mixed-specifier imports (`import { type Foo, Bar }`) still fire because the runtime dependency on `Bar` is real. Type-only re-exports surface as `SideEffect` ImportedSymbols carrying the re-export's `is_type_only` flag (`build.rs:109-118`), so the same predicate covers them; the boundary detector does NOT special-case the import-vs-re-export distinction. Implementation lives in `ResolvedBoundaryConfig::is_type_only_allowed` plus `ModuleGraph::outgoing_edge_summaries` (single-pass walk over the edge range returning target + all-type-only + first-span-start). `tracing::debug!` line on every allowed crossing for support diagnostics; no human-facing log. No preset defaults the field on. Strictly additive via `#[serde(default, skip_serializing_if = "Vec::is_empty")]`. Test matrix at `crates/core/tests/integration_test/boundary_violations.rs::allow_type_only_*` plus the `boundary-type-only` fixture exercising whole-decl, inline-type, namespace-type, mixed, side-effect, plain-value, and type-only re-export shapes.
- **Static test coverage gaps**: module graph reachability from test entry points to runtime files/exports. Not line-level coverage. Test files are identified by plugin patterns and production-mode exclusion rules. Reports runtime files with no test dependency path and runtime exports with no test-reachable reference chain. Gated by `--coverage-gaps` flag on the health command and `coverage-gaps` rule severity.
- **Complexity suppression**: health command complexity findings (functions and synthetic Angular template entries exceeding cyclomatic/cognitive thresholds) support inline suppression via `// fallow-ignore-next-line complexity` and `// fallow-ignore-file complexity`. The blanket `// fallow-ignore-next-line` (no kind) also suppresses complexity findings on the next line. The check happens in `collect_findings()` in `crates/cli/src/health/mod.rs` using `suppress::is_suppressed()` with `IssueKind::Complexity` (discriminant 18). File scores and vital signs are NOT affected by suppressions (they reflect actual complexity, not alerting). Synthetic Angular `<template>` findings in `.html` files get file-level suppression actions because line comments cannot be expressed in HTML; inline template findings in `.ts` files keep line-level suppression actions anchored at the decorator. See issue #108 (till's comment).
- **Synthetic Angular `<template>` complexity**: `health --complexity` emits synthetic `<template>` findings for Angular template control-flow density. External `templateUrl` files (`./foo.component.html`) are scanned at `parse_html_to_module_with_complexity` time and report against the `.html` file's coordinates. Inline `@Component({ template: \`...\` })` decorators are captured during the visitor pass (`ModuleInfoExtractor::inline_template_findings`) and synthesised after the visit by `parse::append_inline_template_complexity`, with the finding's line/col remapped onto the host `.ts` file's `@Component`/`@Directive` decorator start so jump-to-source lands on the decorator. This anchoring also lets `// fallow-ignore-next-line complexity` placed directly above the decorator suppress through the existing health-side check, with no special-case suppression plumbing. The CLI emits `suppress-line` actions for inline-template findings (matching JS/TS function complexity) instead of the HTML `suppress-file` action used for external templates. Template literals containing `${...}` expressions and `template:` properties bound to a variable are skipped (out of scope for the first cut). **CRAP scoring on external `templateUrl` findings inherits from the owning `.component.ts`** (`build_template_inherit_contexts` in `crates/cli/src/health/scoring.rs`): for each `.html` module carrying a `<template>` entry, the scoring loop walks `graph.reverse_deps` to enumerate every non-test `.ts` importer, ORs their `is_test_reachable` bits, unions their `test_referenced_exports`, picks the first test-reachable owner for human-output provenance, and runs `compute_crap_scores_estimated` against that combined context with `CoverageSource::EstimatedComponentInherited`. The resulting per-function CRAP carries a `coverage_source: "estimated_component_inherited"` discriminator and an `inherited_from: "<owner>.component.ts"` provenance field; the human formatter renders `(inherited from foo.component.ts)` after the CRAP score; the JSON `increase-coverage` action targets the `.ts` owner path via a new `target_path` field so AI agents add tests on the component rather than scaffolding tests against the structurally untestable `.html`. Score itself does not move for the common case (BFS already cascades reachability through the `templateUrl` `SideEffect` edge), so existing baselines and `--max-crap` gates are unaffected. Tier 2 (AOT source-map back-mapping) will introduce a `measured_aot_source_map` variant on `CoverageSource`. See issues #183, #186, #187.
- **CRAP score threshold**: `--max-crap` (health and audit, default 30.0, `health.maxCrap` in config) compares each function's CRAP score (`CC^2 * (1 - cov/100)^3 + CC`) against the threshold. Per-function CRAP is produced as a side effect of file scoring (`compute_file_scores` in `scoring.rs`) and merged into `HealthFinding` by `merge_crap_findings` in `health/mod.rs`. Uses Istanbul per-function coverage when `--coverage` resolves; falls back to a graph-based estimator (directly test-referenced = 85%, indirectly reachable = 40%, untested = 0%). Findings triggered by CRAP alone, or alongside cyclomatic/cognitive, carry populated `crap` and (when Istanbul matched) `coverage_pct` fields and share the `complexity` suppression kind. See issue #141.
- **Script binary-to-package resolution**: npm `scripts` binary names are mapped to package names via a 4-step chain: (1) static `BINARY_TO_PACKAGE` table for known divergences (`tsc` -> `typescript`, `ng` -> `@angular/cli`, etc.), (2) `node_modules/.bin/<binary>` symlink target path extraction, (3) dynamic reverse map built from each dependency's `package.json` `bin` field (handles both string `"bin": "./cli.js"` and object `"bin": {"attw": "./cli.js"}` forms), (4) identity fallback (binary = package name). The dynamic map probes `node_modules/` at both the project root and each workspace root, handling non-hoisted setups (pnpm strict, Yarn workspaces). Built once per analysis run. See discussion #107. Positional file arguments (`tsc ./scripts/deploy.ts`, `node ./scripts/deploy.js`) extracted from script values are seeded as entry patterns to keep CI-only / deploy-only sources reachable. For workspace packages, paths are joined with the workspace's project-relative prefix and `..` segments are normalized via `normalize_script_entry_pattern`; `..` segments that escape the project root are skipped instead of producing unmatchable globs (issue #195 Case C).
- **Vite `css.preprocessorOptions.{scss,sass,less,stylus}.additionalData`**: the Vite plugin extracts the string value at each preprocessor's `additionalData` key from `vite.config.{ts,js}` and scans it for stylesheet imports via the same regex used by the CSS file parser. SCSS / Sass bodies seed discovered local style paths as entry patterns and use SCSS-aware normalization (extensionless local specifiers become `./X` partials); package imports such as `bootstrap` and `bootstrap/scss/functions`, plus Tailwind CSS `@plugin "pkg"` directives, are credited as referenced dependencies instead of inert entry globs when no matching local style candidate exists. Less / Stylus strings are parsed for `@import "path"` and package dependency credit, but local `.less` / `.stylus` files are not currently part of the discovered source-file set. Function-form `additionalData: (source, file) => ...` is silently skipped (out of static-analysis scope). Specifiers are stripped of leading `./` before push because globset matches against project-relative paths without that prefix. See issue #195 Case A.
- **Cypress `e2e.specPattern` / `component.specPattern` / `supportFile`**: the Cypress plugin reads `cypress.config.{ts,js,mjs,cjs}` and seeds `e2e.specPattern`, `component.specPattern`, `e2e.supportFile`, and `component.supportFile` (string or array of glob patterns / paths) as entry patterns. Static patterns under `**/*.cy.{ts,tsx,js,jsx}`, `cypress/**/*.{ts,tsx,js,jsx}`, and `cypress/support/**/*.{ts,js}` seed Cypress's default component spec discovery and default Cypress 9-style layout; the config-driven extraction handles Cypress 10+ projects whose specs live outside `cypress/` (e.g. `tests/**/*.cy.ts`, `src/**/*.cy.ts`). See issue #195 Case E.
- **Script multiplexer handling**: `concurrently`, `npm-run-all`, `npm-run-all2`, `run-s`, `run-p`, `run-s2`, `run-p2` are recognized as script multiplexers. Their positional arguments are treated as script names (not binary names or file paths) and excluded from `used_packages`. Only the multiplexer binary itself is recorded. This prevents false "unused dependency" reports for packages referenced only via `npm:` prefixed script delegation (e.g., `concurrently "npm:dev" "npm:test"`).
- **Entry export validation** (`--include-entry-exports`): when enabled, entry file exports (package.json main/module/exports, framework convention files) are subject to unused-export detection instead of being automatically marked as used. Catches typos in framework exports (e.g., `meatdata` instead of `metadata` in Next.js pages). Still respects plugin `used_exports` rules for partial checking. CLI-only flag, not in config schema.
- **Single-file filtering** (`--file`): scopes check output to specified file(s). Full graph still builds (correct results require cross-file analysis), but only issues in matching files are reported. Dependency-level issues (unused deps, dev deps, type-only deps) are suppressed in file mode since they are project-wide properties. Warns when a path is not found. Useful for lint-staged pre-commit hooks.
- **Hotspot ownership signals** (`--ownership` on `health --hotspots`): per-file ownership metrics derived from `git log --use-mailmap --format='%at|%ae'`. Computed only when `--hotspots` runs and a file passes the `min_commits` threshold. Signals: bus factor (Avelino truck factor — minimum contributors covering ≥50% of recency-weighted commits), contributor count (after bot-pattern filtering), top contributor with stale-days, recent contributors (top-3), declared CODEOWNERS owner, drift (file age ≥30d AND original author share <10%), tristate `unowned` (true=no rule matches; false=rule matches; null=no CODEOWNERS file). Emitted via `OwnershipMetrics` on `HotspotEntry` (`skip_serializing_if = "Option::is_none"` for backwards compat). JSON injects ownership-aware actions: `low-bus-factor`, `unowned-hotspot` (with synthesized CODEOWNERS pattern), `ownership-drift`. Email privacy mode (`ownership.emailMode` config or `--ownership-emails` CLI): `handle` (default, local-part only with GitHub noreply unwrap), `hash` (`xxh3:` non-cryptographic pseudonym), `raw`. Bot patterns are globset matches against the raw author email; defaults cover `*\[bot\]*` (escaped brackets), `dependabot*`, `renovate*`, `github-actions*`, `svc-*`, `*-service-account*`. `*noreply*` is intentionally NOT a default because most human GitHub contributors commit from `<id>+<handle>@users.noreply.github.com` (GitHub's privacy default); the actual bot accounts already match via `\[bot\]`. Author emails are interned in `ChurnResult.author_pool` and per-file maps reference them by `u32` index to keep the bitcode cache compact. Cache version bumped to v2.
- **Feature flag detection**: AST-based detection of feature flag patterns. Three categories: (1) environment variables (`process.env.FEATURE_*` with 12 built-in prefixes), (2) SDK calls (25+ built-in patterns for LaunchDarkly, Statsig, Unleash, GrowthBook, Split, ConfigCat, Flagsmith), (3) config objects (opt-in heuristic for objects named "feature", "flag", "toggle"). Reports flag locations, detection confidence (high/low), guard spans. Cross-references with dead code: flags guarding unused exports are highlighted. Configured via `flags` section in config. Default severity: `off`. CLI: `fallow flags`. Inline suppression: `// fallow-ignore-next-line feature-flag` (line-level) and `// fallow-ignore-file feature-flag` (file-level). The suppression check runs in `crates/cli/src/flags.rs` during both built-in and custom flag collection loops, using `is_suppressed()` / `is_file_suppressed()` with `IssueKind::FeatureFlag`. The file-level check is hoisted outside the per-flag loop for efficiency.
- **File encoding contract for fix + extract** (issue #475): fallow's contract is "UTF-8 with or without BOM; line offsets are computed against the post-BOM view; the BOM, if present on input, is preserved on output by `fallow fix`." `crates/extract/src/lib.rs` strips the leading UTF-8 BOM at every file-read entry point (`parse_single_file_cached`, `parse_single_file`, `parse_from_content`) before computing `content_hash` (xxh3 over post-strip bytes) and before passing source to `parse_source_to_module`. `parse_source_to_module` carries a defense-in-depth `strip_bom` call for out-of-tree callers (fuzzers, integration fixtures, future embedders). `crates/cli/src/fix/io.rs::read_source` returns `Result<Option<(String, EncodingMetadata)>, EncodingError>` where `EncodingMetadata { line_ending, had_bom }` captures the round-trip shape; `stage_fixed_content` re-prepends the BOM bytes (`EF BB BF`) on write when `had_bom`. Mixed CRLF/LF files (both `\r\n` and bare-LF lines present) are rejected with `EncodingError::MixedLineEndings`; the orchestrator translates this into `SkipReason::MixedLineEndings`, surfaces an additive `skipped_mixed_line_endings: M` envelope counter, emits a stderr line naming `dos2unix` and `git config core.autocrlf input` as remediation, and exits with code 2. The skip is NOT self-healing: re-running fallow alone does not normalize the file. Cross-fixer composition preserves the `had_bom` flag because the staged-content fast path re-runs `classify_source` on the staged bytes (which already include the re-prepended BOM from the first fixer). The extraction cache (`CACHE_VERSION` bumped from 88 to 89) invalidates automatically on upgrade because pre-fix caches keyed hashes over BOM-included bytes. The `fix_preview` and `fix_apply` MCP tool descriptions flag the non-self-healing nature for AI agents.

- **Unused pnpm catalog entries**: `pnpm-workspace.yaml`'s `catalog:` map (default catalog) and `catalogs.<name>:` maps (named catalogs) are parsed via `serde_yml` plus a section-aware line scanner (`crates/config/src/workspace/pnpm_catalog.rs`) that records the 1-based source line for every entry. Workspace `package.json` files are walked for dependency values matching the `catalog:` protocol: a bare `"react": "catalog:"` and an explicit `"react": "catalog:default"` both resolve to the default catalog per pnpm's spec; named references use the catalog name verbatim. The detector (`crates/core/src/analyze/unused_catalog.rs::find_unused_catalog_entries`) emits one `UnusedCatalogEntry` per catalog entry no workspace `package.json` references via `catalog:` for the matching catalog group. Each finding also carries `hardcoded_consumers: Vec<PathBuf>`, the workspace `package.json` paths that declare the same package with a non-`catalog:` version range, so consumers can decide whether to delete the catalog entry or switch the consumer to `catalog:` first (the differentiator vs knip's catalog detection). Output sort is `(path, catalog_name, entry_name)` with the default catalog sorting first via `catalog_sort_key`. Default severity `warn` (not `error`) because catalog entries ship zero bytes; matches the rest of the `unused_dev_dependencies` / `unused_optional_dependencies` family. JSON `actions[]` emits a YAML-style `# fallow-ignore-next-line unused-catalog-entry` suppression action via the new `SuppressKind::YamlComment`. Filter-to-workspaces clears catalog findings (whole-project concern, not per-workspace). **Auto-fix** (`crates/cli/src/fix/catalog.rs`, issues #335 and #360): `fallow fix` removes the entry by line-aware deletion (no YAML round-trip writer in the workspace; a full reprint via `serde_yaml_ng` would obliterate comments). Object-form entries (`react:\n    specifier: ^18.2.0`) are detected by consuming subsequent lines whose indent is strictly greater than the entry's own; blank lines stop the consumption to preserve inter-entry whitespace. `fix.catalog.deletePrecedingComments` controls contiguous same-indent YAML comment blocks immediately above removed entries: `auto` (default) deletes blocks that directly follow the parent catalog header or a blank separator, `always` deletes any adjacent block, and `never` preserves leading comments. Two escape hatches keep curated comments safe regardless of policy: any line in the block bearing the `# fallow-keep` marker bails out of deletion (mirrors the `fallow-ignore` inline-suppression convention), and the `auto` policy additionally preserves section-banner blocks whose comment body starts with three or more `=`, `-`, `*`, `_`, `~`, `+`, or `#` characters (`# === React 18 production pins ===`). The JSON `remove_catalog_entry` action carries both `line` (first deleted line, the comment when the policy absorbs one) and `entry_line` (the entry's original 1-based line) so CI annotators and dedup caches keep a stable anchor across policy changes; the human stderr summary appends `(+M catalog comment lines)` after the fixed-issue count when at least one preceding comment line was absorbed. Entries with non-empty `hardcoded_consumers` are skipped (a record with `applied: false`, `skipped: true`, `skip_reason: "hardcoded_consumers"` is emitted so the user knows to migrate the consumer first). Multi-document YAML files (`---` separator) are rejected up front with `skip_reason: "multi_document_yaml"`. When removing the last entry of a catalog group (default `catalog:` or a named `catalogs.<name>:`) leaves the header with no children, the fix rewrites the header to `catalog: {}` / `<name>: {}` instead of leaving bare `key:`, because bare-key parses as null in YAML and pnpm rejects null-valued catalogs with `Cannot convert undefined or null to object` at install time (verified against pnpm 10.33.4). The post-edit content is reparsed with `serde_yaml_ng` as a syntactic sanity gate before persisting. The per-instance `auto_fixable` bool in the JSON `actions[]` flips to `false` for entries with hardcoded consumers (computed in `build_actions` in `crates/cli/src/report/json.rs`, mirroring the existing per-instance pattern used by `unresolved_catalog_references`); agents that filter on the bool no longer apply destructive ops to entries needing consumer-side migration first. After a successful fix that touched `pnpm-workspace.yaml`, the human stderr emits a one-line `Run \`pnpm install\` to refresh pnpm-lock.yaml` reminder. The LSP code action (`crates/lsp/src/code_actions/quick_fix.rs::build_remove_catalog_entry_actions`) mirrors the same guard and uses anchored key-prefix matching to reject substring collisions between sibling catalog entries with shared prefixes (`react` vs `react-native`), but intentionally does not delete leading comments so editor quick fixes stay conservative. See issues #329 (detection), #335 (auto-fix), and #360 (leading comments). See also: the `unresolved-catalog-reference` inverse rule (consumer references a catalog that does not declare the package).
- **Empty pnpm catalog groups**: named `catalogs.<name>:` groups in `pnpm-workspace.yaml` that contain no package entries are detected by `crates/core/src/analyze/unused_catalog.rs::find_empty_catalog_groups`. The top-level `catalog:` map is intentionally ignored even when empty, because it is the default-catalog hook and can be a harmless placeholder. Findings are project-root-relative to `pnpm-workspace.yaml`, sorted by `(path, catalog_name)`, default to `warn`, support YAML line suppressions via `# fallow-ignore-next-line empty-catalog-group`, and are auto-fixable by removing the empty named group header without reprinting the whole YAML document. Changed-file and audit attribution paths must account for the relative manifest path.
- **Unresolved pnpm catalog references**: the inverse of `unused-catalog-entries`. The same YAML parse + consumer walk (`find_unresolved_catalog_references` in `crates/core/src/analyze/unused_catalog.rs`) iterates over every `catalog:` / `catalog:<name>` reference collected in `CatalogConsumers.referenced_with_locations` and emits an `UnresolvedCatalogReference` for any `(catalog_name, package_name)` pair the YAML does not declare. The consumer-side line number is captured by a section-aware `scan_dep_lines` pass over the raw `package.json` source that tracks brace depth so nested-object keys (`peerDependenciesMeta.<pkg>.optional`) cannot be misread as direct dep entries. Each finding carries `available_in_catalogs: Vec<String>`: other catalogs in the same workspace that DO declare the package, sorted lexicographically; agents flip the reference to one of those when non-empty, otherwise add the missing entry to the named catalog. Default severity `error` (matches `unresolved-imports`): `pnpm install` errors with `ERR_PNPM_CATALOG_ENTRY_NOT_FOUND_FOR_CATALOG_PROTOCOL` so the static finding catches the same failure pre-install. Default-catalog rendering uses a special-case "default catalog" phrasing in human / SARIF / LSP / CodeClimate output instead of `catalog 'default'` because users who write bare `catalog:` think of it as "the catalog", not as a named one. Suppression is config-only via the new `IgnoreCatalogReferenceRule { package, catalog?, consumer? }` array on `FallowConfig` (consumer is a glob, all three fields AND together when set); `package.json` does not support comments so inline suppression is structurally impossible. JSON `actions[]` is discriminated by the `available_in_catalogs` shape: `update-catalog-reference` (primary when alternatives exist), `add-catalog-entry` (primary when none do), `remove-catalog-reference` (fallback from the generic spec), plus an `add-to-config` for `ignoreCatalogReferences` with a paste-ready value scoped to the consumer. Filter-to-workspaces retains findings under the active set because each finding is anchored at a consumer `package.json` (unlike unused-catalog-entries which lives in the root YAML). The LSP diagnostic mirrors the catalog-entries pattern with `root.join(&finding.path)` so the URI built from the project-relative path is absolute. See issue #334.
- **Unused pnpm dependency overrides**: pnpm's `overrides:` section (top-level in `pnpm-workspace.yaml` for pnpm 9+, or legacy `pnpm.overrides` in the root `package.json`) lets a workspace force a specific transitive dependency version (typically for a CVE patch). The detector (`crates/core/src/analyze/unused_overrides.rs::find_unused_dependency_overrides`) parses both source forms via `crates/config/src/workspace/pnpm_overrides.rs`, walks every workspace `package.json` to compute the union of declared package names across `dependencies`, `devDependencies`, `peerDependencies`, and `optionalDependencies`, and ALSO parses `pnpm-lock.yaml` to collect every package name appearing in `packages:` / `snapshots:` keys plus any nested `dependencies` / `optionalDependencies` / `devDependencies` / `peerDependencies` map. An `UnusedDependencyOverride` is emitted only for overrides whose target package is absent from BOTH the declared set AND the lockfile-resolved set. Parent-chain shapes (`react>react-dom: ^17`) are USED when EITHER the parent OR the target is declared OR resolved in the lockfile, covering the CVE-fix pattern where the override forces a transitive version that only appears in `pnpm-lock.yaml`. When the lockfile is missing or malformed (`serde_yaml_ng::from_str` fails), the detector degrades to the package.json-only fallback and the `hint` field reminds consumers to verify the override before removal. Lockfile keys are normalized via `package_name_from_lock_key`, which strips the leading `/` used by pnpm 6 keys, the version selector at `@`, and pnpm peer suffixes in parentheses, correctly handling scoped packages (`@scope/pkg@1.0.0` → `@scope/pkg`). Default severity `warn`. Each finding decomposes the raw key into `target_package`, `parent_package` (Option), `version_constraint` (Option, e.g. `Some("<18")` for `@types/react@<18`), and `version_range` (the forced version). Suppression is config-only via `ignoreDependencyOverrides: [{ package, source? }]` on `FallowConfig`: `package` matches the override's `target_package` exactly, optional `source` scopes to `"pnpm-workspace.yaml"` or `"package.json"`. JSON `actions[]` emits `remove-dependency-override` as primary plus an `add-to-config` for `ignoreDependencyOverrides`. Filter-to-workspaces clears findings (whole-project concern). See also: `misconfigured-dependency-overrides` (the entry-shape variant). See issues #336 (detector) and #371 (lockfile-aware check).
- **Misconfigured pnpm dependency overrides**: same parser as `unused-dependency-overrides`, different gate. `find_misconfigured_dependency_overrides` emits `MisconfiguredDependencyOverride` for entries whose key cannot be parsed (`DependencyOverrideMisconfigReason::UnparsableKey`: empty key, dangling separators like `react>`, malformed selectors like `@types/react@<<18`) or whose value is missing/empty (`EmptyValue`). pnpm itself refuses to honor these at install time. The pnpm idiom values `-` (delete), `$ref` (self-reference), and `npm:alias@^1` (npm alias) are explicitly allowlisted and never flagged. Default severity `error`: a malformed override breaks `pnpm install` for every contributor. Findings carry the raw key, raw value, the reason discriminant (serialized as kebab-case `unparsable-key` / `empty-value`), source, path, and line. Suppression: same `ignoreDependencyOverrides` config-only rule as `unused-dependency-overrides`. JSON `actions[]` emits `fix-dependency-override` as primary plus the same `add-to-config` suppress. Filter-to-workspaces clears findings. See also: `unused-dependency-overrides` (the consumer-not-declared variant). See issue #336.
