import Footer from "@theme-original/Footer";

export default function FooterWrapper(props: Record<string, unknown>) {
  return <Footer {...props} />;
}

export const unusedFooterHelper = "unused-footer-helper";
