export enum Color {
    Red = 'red',
    Green = 'green',
    Blue = 'blue',
}

export enum HttpStatus {
    Ok = 200,
    NotFound = 404,
    InternalError = 500,
    BadGateway = 502,
}

export enum LogLevel {
    Debug = 0,
    Info = 1,
    Warn = 2,
    Error = 3,
}
