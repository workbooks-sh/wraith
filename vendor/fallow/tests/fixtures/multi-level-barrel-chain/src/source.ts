export const alpha = 'used through 3-level chain';
export const beta = 'also used through 3-level chain';
export const gamma = 'only re-exported through barrel-a but never imported';
export const delta = 'only re-exported through barrel-b, not barrel-a';
export const epsilon = 'not re-exported at all';
