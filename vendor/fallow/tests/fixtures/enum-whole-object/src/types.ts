export enum Status {
    Active = 'active',
    Inactive = 'inactive',
    Pending = 'pending',
}

export enum Direction {
    Up = 'up',
    Down = 'down',
    Left = 'left',
    Right = 'right',
}

export enum Color {
    Red = 'red',
    Green = 'green',
    Blue = 'blue',
}

export enum Priority {
    Low = 1,
    Medium = 2,
    High = 3,
}
